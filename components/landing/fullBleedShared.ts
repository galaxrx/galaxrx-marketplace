/** Shared with `LandingSlideshow` fullBleed and the homepage hero — same vertical rhythm and photo treatment. */
export const FULL_BLEED_MIN_H =
  "min-h-[20rem] sm:min-h-[22rem] md:min-h-[min(48vh,34rem)] lg:min-h-[min(65vh,48rem)] xl:min-h-[min(70vh,54rem)] 2xl:min-h-[min(75vh,60rem)]";
